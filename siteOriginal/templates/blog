<!DOCTYPE HTML>
<html>
    {{> head}}
    <body class="default">
        <div id="wrap">
            <div id="main">
                {{> header}}
                <div class="container">
                    <h1>{{title}}</h1>
                    {{> main }}
                    {{> blog-footer}}
                </div>
            </div>
        </div>
        {{> footer}}
    </body>
</html>
